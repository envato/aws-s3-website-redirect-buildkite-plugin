services:
  tests:
    image: "buildkite/plugin-tester"
    volumes:
      - ".:/plugin:ro"
  lint:
    image: "buildkite/plugin-linter"
    command: ["--id", "envato/aws-s3-website-redirect"]
    volumes:
      - ".:/plugin:ro"
